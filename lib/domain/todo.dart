class Todo {
  const Todo({
    required this.uid,
    // required this.assigned,
    required this.name,
    required this.description,
  });

  final String uid;
  // final User? assigned;
  final String name;
  final String description;
}
